import { Button } from 'flowbite-react';

// Check if CustomFlowbiteTheme is available in 'flowbite-react'
// If not, you might need to define it yourself or use the correct import.


export default function PButton() {
  return (
    <Button role='submit' className="">
      Click me
    </Button>
  );
}
